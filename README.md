# RESQ-disaster-response-simulator
Offline C/C++ disaster rescue &amp; emergency response simulator — severity-based triage (priority queue), resource allocation, and shortest-route dispatch (Dijkstra/BFS) with priority vs. FCFS comparison.
